# Net Worth UX Notes

1. The top `Patrimonio` panel now contains both the donut/composition block and the temporal evolution chart.
2. Category selection comes only from the composition block; the old standalone category pills are no longer part of the primary flow.
3. When a category is selected, the temporal chart exposes a contextual position selector so the user can switch from category-level evolution to a concrete asset or liability.
