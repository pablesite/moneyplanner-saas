export type AppCapabilities = {
  isPremium: boolean;
  people: boolean;
  ownership: boolean;
};

export const capabilities: AppCapabilities = {
  isPremium: true,
  people: true,
  ownership: true,
};
